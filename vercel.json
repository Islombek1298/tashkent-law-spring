{
  "version": 2,
  "buildCommand": "npm install --no-audit --no-fund && vite build",
  "outputDirectory": "dist",
  "installCommand": "npm install --no-audit --no-fund",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
